<Window x:Class="WpfMultilingual.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMultilingual"
        xmlns:localization="clr-namespace:WpfMultilingual.Localization"
        mc:Ignorable="d"
        Title="MainWindow" Width="400" SizeToContent="Height">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!--Language selection-->
        <Label Grid.Row="0" Grid.Column="0"
               Content="Language" />
        <ComboBox Grid.Row="0" Grid.Column="1"
                  ItemsSource="{Binding Languages}"
                  SelectedItem="{Binding Language}"
                  DisplayMemberPath="NativeName"/>

        <!--Label with translated string-->
        <Label Grid.Row="1" Grid.Column="0"
               Content="Translation" />
        <Label Grid.Row="1" Grid.Column="1"
               Content="{localization:Translate HelloWorld}" />

        <!--Label with translated string-->
        <Label Grid.Row="2" Grid.Column="0"
               Content="Translation non-existent" />
        <Label Grid.Row="2" Grid.Column="1"
               Content="{localization:Translate NotExistingResource}" />

</Grid>
</Window>
